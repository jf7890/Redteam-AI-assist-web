# These variables are injected into /env.js at container start (runtime config).
# IMPORTANT: these URLs are used by the browser.
# - 127.0.0.1 always means "the machine running the browser"
# - If AI server is remote, change AI_BASE_URL to that server IP/hostname.

AI_BASE_URL=http://127.0.0.1:8088
LOCAL_AGENT_URL=http://127.0.0.1:8787

DEFAULT_TARGETS=172.16.100.128,dvwa.local
DEFAULT_POLICY_ID=lab-default

DEFAULT_TENANT_ID=lab
DEFAULT_USER_ID=student
DEFAULT_AGENT_ID=redteam-ai-assist
